/*
1. Do WiFi scan
2. Sort access points in decreasing order
 */
package com.example.app2;

public class ScanWiFi {
    // Scan wifi, store the results in rss_scan

    // Sort the access points
    public int[] sort_rss_scan(float[] rss_scan){
        int numMac = rss_scan.length;
        int[] sorted_index = new int[numMac]; // why not return sort macs?
        return sorted_index;
    }
}
